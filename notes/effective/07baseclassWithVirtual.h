#ifndef BASECLASSWITHVIRTUAL_H
#define BASECLASSWITHVIRTUAL_H
/*
 *Declare destructors virtual in polymorphic base classes
 *条款07：为多态基类声明virtual析构函数
 *polymorphic(带多态性质的)base classes 应该声明一个virtual析构函数。如果class 带有任何virtual函数，
 *它就应该拥有一个virtual析构函数
 *Classes 的设计目的如果不是作为base classes使用，或不不是为了具备多态性，就不应该声明virtual析构函数
 */





#endif //BASECLASSWITHVIRTUAL_H
